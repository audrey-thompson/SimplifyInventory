Module 3 Done:
 - Made separate create account page
 - Added bottom menu
 - Added account settings page

Module 4 Done:
- Rearranged menu, adding home and shop pages (home, inventory, shop, settings)

Module 4 Revisited Done:
//// Password Hashing ////
- added .trim() to remove whitespace to front and end of string in username for account creation
- added check for blank username or password before account creation
- added lifecycleScope.launch because the insertUser is a suspend function and needs
    to run inside a coroutine
- passwords are now hashed (with salts) and stored as hashed passwords on the creation of an account
- deleted a query in inventoryitemdao that was not being used because we didn't need to ask the
    database what an inventory item's item type was since it already has that as a field
- add password hash to login password so it actually works by checking hashed version of the
    password the user enters instead of the cleartext password
- add .trim() to login username

//// Sort ////

//// Search ////

Module 5 Databases Done:
 - Updated User table to require more than just Username and Password
 - created dao folder(instead of just databse stuff in loginhelpers)
   (InvntoryItemDao, ItemTypeDao, RfidDeviceDao, RfidTagDao, UserDao)
 - Created AppDatabase.kt
 - created entity folder
   (User, RfidDevice, ItemType, InventoryItem, RfidTag)
 - Removed sort functionality
 - Change plan, only items with RFID tags are tracked and the number is just a reflection of the
    number of items being tracked, unchangeable by the user

Final Preparation Done:
 - UI, UI, UI
 - Remove up and down menus 
 - Change bottom navigation so it isn't rewritten on every page 

